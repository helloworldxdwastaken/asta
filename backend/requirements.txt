# Asta backend — Python 3.11+
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.5.0
pydantic-settings>=2.1.0
httpx>=0.26.0
aiofiles>=23.2.0

# AI
openai>=1.12.0
google-generativeai>=0.3.0
anthropic>=0.18.0

# Telegram
python-telegram-bot>=21.0

# RAG (Chroma; embeddings via Ollama or Chroma default)
chromadb>=0.4.22

# Scheduler
apscheduler>=3.10.0

# DB
aiosqlite>=0.19.0

# Google Drive (OAuth)
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# Web search (no API key; multi-backend: bing, brave, duckduckgo, etc.)
ddgs>=9.0.0

# Timezone from lat/lon (offline, no API)
timezonefinder>=6.0.0

# Audio → text (local, free)
faster-whisper>=1.0.0
